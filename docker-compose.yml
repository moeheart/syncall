services:
  postgres:
    image: postgres:16
    container_name: syncall-postgres
    environment:
      POSTGRES_DB: syncall
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - syncall-postgres:/var/lib/postgresql/data

volumes:
  syncall-postgres:
